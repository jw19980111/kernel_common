name: 调试工作流
on: [push]

jobs:
  debug-job:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      # 这里是关键：开启调试会话
      - name: 开启调试会话
        uses: mxschmitt/action-tmate@v3
